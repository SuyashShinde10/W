var mysql=require('mysql');
var con=mysql.createConnection({
    host:'localhost',
    user:'root',
    password:'',
    
})

con.connect(function(err){
    if(err){throw err;}
    console.log("connected");
    sql1='CREATE DATABASE employee2';
    con.query(sql1,function(err,result){
        if(err)throw err
        console.log('database created');
    })
})